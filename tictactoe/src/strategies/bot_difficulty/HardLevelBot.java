package strategies.bot_difficulty;

import entities.Board;
import entities.Move;
import entities.Player;

public class HardLevelBot implements Bot{
    @Override
    public Move makeMove(Board board, Player player) {
        return null;
    }
}
